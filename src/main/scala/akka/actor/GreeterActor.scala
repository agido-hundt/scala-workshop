package akka.actor

/**
 * Greeter actor which answers to a greet
 */
class GreeterActor extends Actor with ActorLogging{
  override def receive: Receive = {
    //Receive Greet message log something and send done message

    ???
  }
}
